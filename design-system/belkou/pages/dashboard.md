# Dashboard (`/dashboard`) — student / “buyer”

- Mesh hero with greeting + quick links (cours, forum, compte, affiliation)
- Stat strip: enrolled / active / avg progress
- Course cards: status chip, progress bar, CTA Continuer/Ouvrir/Finaliser
- Account + affiliate sections keep single-page anchors (`#account`, `#affiliate`)
