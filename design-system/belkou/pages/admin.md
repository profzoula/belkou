# Admin (`/admin`) — seller/ops dashboard

- Sidebar: primary active item, ThemeToggle, token background (`bg-background` / `bg-muted/20`)
- Stats: `AdminStatCard` with brand-accent highlight ring
- Login: mesh backdrop + card form (`rounded-2xl`)
- Avoid hardcoded navy (`#1a2744`) / gray page fills
