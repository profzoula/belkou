# Catalog (`/courses`)

- Hero band with search (single field)
- Filters: price (all/paid/free) + skill level
- Layout toggle: grid / list
- Cards: `CourseCatalogCard` (shared with landing trending)
- Motion: staggered `FadeIn`
- Empty state with reset CTA
